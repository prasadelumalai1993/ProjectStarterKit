# Test Case: Web import produces correct structured fields

**Increment/stage:** Stage 1, Increment 1.1 — Web import

**Exit criteria being verified:** "5 real recipe URLs from 5 different sites import with correct ingredients/steps/servings."

**Setup:** 5 real, publicly reachable recipe URLs, chosen from 5 different sites (at least 2 with schema.org/Recipe markup, at least 2 without, to exercise both the structured-parse path and the heuristic fallback).

**Steps:**
1. Run `importers.web.import_recipe(url)` for each of the 5 URLs.
2. For each result, manually compare the extracted ingredients/steps/servings against the actual source page.

**Expected result:** all 5 recipes have correctly separated ingredients (each with amount/unit/name split out, not left as one string) and steps, and a servings count matching the source page.

**Actual result / status:** not yet run — Increment 1.1 in progress.
