# Roadmap

<If the project has genuinely separate stages (e.g. "core system" vs. "the layer built on top of it"), say so and split accordingly, mirroring project-overview.md's "Stages" section. Otherwise, just one staged list of increments below.>

## Stage 1 — <name>

Goal: <one sentence — what does this stage prove/deliver, that the next stage builds on>.

### Increment 1.0 — <name>
<What gets built. Keep it small enough to be genuinely demoable on its own.>
**Exit criteria:** <a concrete, checkable statement of "done" — not a vibe. Someone else should be able to verify this without asking you what you meant.>

### Increment 1.1 — <name>
...
**Exit criteria:** ...

<Add as many increments as needed. Each one should leave the system in a working state — if a later increment turns out harder than expected, everything before it still works.>

**Stage 1 is done when:** <the overall exit condition for the whole stage, in concrete terms.>

## Stage 2 — <name, if applicable>

...

## Not scheduled yet

<Things explicitly out of scope for now — cross-reference design-choices.md's "Deferred, not rejected" section rather than duplicating the reasoning here.>
