# Non-Functional Requirements

The constraints the system must operate under — performance, privacy, cost, offline-capability, security, whatever actually matters for this project. Give numbers/thresholds where possible; "fast" and "secure" aren't testable, "responds in under 200ms at p95" and "no PII leaves the local machine" are.

- NFR-1: <constraint, with a number or a checkable condition>
- NFR-2: <constraint>
